import React from 'react';
import {StyleSheet, Text, View, TouchableOpacity} from 'react-native';

const FavScreen: React.FC = () => {
  return (
    <View style={{}}>
      <Text style={{}}>FavScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default FavScreen;
